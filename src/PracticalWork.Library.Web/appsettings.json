{
  "App": {
    // Строка подключения к основной базе сервиса (PostgreSql)
    "DbConnectionString": "Host=localhost;Port=5432;Database=library;Username=postgres;Password=135792468",
    // Данные для подключения распределенного кэша (Redis)
    "Redis": {
      "Configuration": "localhost:6379",
      "InstanceName": "LibraryCache_"
    },
    // Данные для подкючения хранилища файлов (MinIO)
    "Minio": {
      "Endpoint": "localhost:9000",
      "AccessKey": "minioadmin",
      "SecretKey": "minioadmin",
      "Bucket": "book-covers"
    },
    "RabbitMQ": {
      "Host": "localhost",
      "User": "rabbit",
      "Password": "rabbit",
      "Exchange": "library.events"
    }

  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
